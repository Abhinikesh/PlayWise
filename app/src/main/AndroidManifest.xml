<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.playwise">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.PlayWise">

        <!-- Splash Launcher Activity -->
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
        </activity>

        <!-- Other Activities -->
        <activity android:name=".FavoritesActivity" />
        <activity android:name=".RulesActivity" />
        <activity android:name=".GroundActivity" />
        <activity android:name=".TipsActivity" />
        <activity android:name=".SportDetailActivity" />
        <activity android:name=".TicketActivity" />
        <activity
            android:name=".quiz.QuizActivity"
            android:label="Sports Quiz"
            android:screenOrientation="portrait"/>
        <activity
            android:name=".quiz.QuizHistoryActivity"
            android:label="Quiz History"
            android:screenOrientation="portrait"/>

    </application>

</manifest>